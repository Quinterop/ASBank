public class currentAccount extends Account {

    public currentAccount(String label, Client client) { //1.2.2 Creation of the CurrentAccount and SavingsAccount
        super(label, client);
    }
    
}
